- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.profile: profile
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.ideavimrc: ideavimrc
    ~/.vimrc: vimrc
    ~/.inputrc: inputrc
    ~/.bash_profile: bash_profile
    ~/.gitconfig: gitconfig


